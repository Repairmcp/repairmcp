{
  "name": "@repairmcp/nhtsa-server",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker hosting the NHTSA MCP server (live recalls/complaints + 49 U.S.C. ch. 301).",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.worker.json",
    "typecheck:worker": "tsc -p tsconfig.worker.json",
    "test": "echo \"(no tests yet)\" && exit 0",
    "lint": "echo \"(no lint yet)\" && exit 0",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@repairmcp/core": "workspace:*",
    "@repairmcp/nhtsa": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "wrangler": "^4.118.0"
  }
}
