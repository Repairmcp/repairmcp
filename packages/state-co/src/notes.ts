/**
 * Colorado's legal-posture lines, produced by the shared templates so states
 * cannot drift from each other.
 */
import {
  EDUCATIONAL_CAVEAT,
  makeEmptySearchHint,
  makeLegalAdviceNote,
} from '@repairmcp/state-law';

export { EDUCATIONAL_CAVEAT };

export const LEGAL_ADVICE_NOTE = makeLegalAdviceNote('Colorado');

export const EMPTY_SEARCH_HINT = makeEmptySearchHint('Colorado');
