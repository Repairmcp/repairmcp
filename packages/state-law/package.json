{
  "name": "@repairmcp/state-law",
  "version": "0.0.0",
  "description": "State-agnostic machinery for RepairMCP state-law verticals: schemas, search, corpus, citation factories, tool builders, and capture contracts. Per-state packages supply profiles, parsers, and data.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint yet)\" && exit 0"
  },
  "dependencies": {
    "@repairmcp/core": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.13"
  }
}
