{
  "name": "@repairmcp/state-tx",
  "version": "0.0.0",
  "description": "Texas state law vertical for RepairMCP: insurance claims practices (Tex. Ins. Code ch. 1952 subch. G anti-steering, ch. 1813 mandatory appraisal, ch. 542 prompt payment with 18% interest, ch. 541 unfair settlement practices with a private action), TDI rules and bulletins, vehicle storage facility and possessory-lien law, the DTPA, and employment rules (the Payday Law), captured verbatim from the Texas statutes, the Texas Administrative Code, and TDI.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" },
    "./data/*": "./data/*"
  },
  "files": ["dist", "data"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint yet)\" && exit 0"
  },
  "dependencies": {
    "@repairmcp/core": "workspace:*",
    "@repairmcp/state-law": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": { "bun-types": "^1.3.13" }
}
